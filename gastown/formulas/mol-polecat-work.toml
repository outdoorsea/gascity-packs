description = """
Polecat work lifecycle — feature-branch variant.

Extends mol-polecat-base with feature-branch workspace setup and
refinery-based submission. The polecat creates a feature branch,
implements the work, then pushes and reassigns to the refinery for
merge review.

## Polecat Contract (Self-Cleaning Model)

1. Receive work (molecule poured with this formula, assigned to you)
2. Follow steps in order (read descriptions, execute, move to next)
3. Submit: push branch, set metadata on work bead, assign to refinery, exit
4. You are GONE — Refinery merges, closes the bead

**No MR beads.** Work beads flow directly: pool → polecat → refinery → closed.
The polecat sets `metadata.branch` and `metadata.target` on the work bead
and reassigns it to the refinery. The refinery merges and closes.

**NEVER CLOSE BEADS.** You must not run `gc bd close` or set status=closed.
Even if you believe the code is already merged, reassign to refinery —
only the refinery verifies merges and closes beads.
`{{base_branch}}` may come from the work bead's own `metadata.target` or
be inherited from a parent convoy with `metadata.target` set.

## Variables

| Variable | Source | Description |
|----------|--------|-------------|
| binding_prefix | import binding | Agent identity prefix for bound Gas Town imports. |
| affected_tests_command | rig `formula_vars` | Shell-safe command that reads `git diff --name-only origin/{{base_branch}}...HEAD` and runs the matching test subset. Empty = run full `test_command`. |

**Rejection-aware.** If the work bead has `metadata.branch` and
`metadata.rejection_reason`, a previous attempt was rejected by the
refinery. Resume the existing branch — don't redo all the work.

**Staleness-aware.** `workspace-setup` runs an already-delivered gate before
any build: if the bead's deliverable is already on the target branch, the
polecat halts with the evidence instead of producing an empty branch for the
refinery to refuse. The gate reports; the polecat confirms by reading the cited
commit before halting, because a false positive drops real work.

**Patch-shape-aware.** `submit-and-exit` reconciles `metadata.no_code_change`
against the branch it is about to hand off: a flagged bead that produced commits
has the flag cleared (a patch that exists must merge), and a flagged bead with a
genuinely empty branch is annotated with the deliverable's evidence so the
refinery can route it out of the merge queue instead of halting on it.

## Failure Modes

| Situation | Action |
|-----------|--------|
| Deliverable already on target | Confirm it, then halt with evidence. Never hand an empty branch to the refinery. |
| Deliverable is not a patch | Reconcile `no_code_change` with the real diff, attach the evidence, hand off. Never set the flag to escape a failing build. |
| Tests fail | Fix them. Do not proceed with failures. |
| Blocked on external | Mail Witness, mark yourself stuck |
| Context filling | `gc runtime request-restart` (blocks until controller kills you) |
| Unsure what to do | Mail Witness, don't guess |"""
formula = "mol-polecat-work"
extends = ["mol-polecat-base"]

[requires]
formula_compiler = ">=2.0.0"

[vars]
[vars.binding_prefix]
description = "Import binding prefix for gastown agent identities, including trailing dot when bound."
default = ""

[vars.affected_tests_command]
description = "Shell-safe affected-tests command. Must read `git diff --name-only origin/{{base_branch}}...HEAD` and run the matching test subset. From rig `formula_vars` or empty to run full test_command."
default = ""

[[steps]]
id = "workspace-setup"
title = "Set up worktree and feature branch"
needs = ["load-context"]
description = """
Ensure you have an isolated git worktree and a clean feature branch.
Every check is idempotent — safe to re-run after crash/restart.

**Config: base_branch = {{base_branch}}**
**Config: setup_command = {{setup_command}}**

`{{base_branch}}` is resolved by `gc sling` in this order:
1. `metadata.target` on the work bead
2. `metadata.target` on the parent convoy chain
3. the rig repo's default branch

**1. Fetch latest and derive your work bead:**
```bash
git fetch --prune origin
CONVOY_STATUS=$(gc convoy status {{convoy_id}} --json)
WORK_BEAD_ID=$(printf '%s' "$CONVOY_STATUS" | jq -r 'if (.children | length) == 1 then .children[0].id else empty end')
if [ -z "$WORK_BEAD_ID" ]; then
  echo "mol-polecat-work requires an input convoy with exactly one tracked member" >&2
  gc runtime drain-ack
  exit 1
fi
```

**2. Ensure worktree exists.**

Check if `metadata.work_dir` already records your worktree path:
```bash
WORKTREE=$(gc bd show "$WORK_BEAD_ID" --json | jq -r '.[0].metadata.work_dir // empty')
```

**If worktree path exists in metadata** — reuse it:
```bash
cd "$WORKTREE"              # Enter existing worktree
```
If the directory is missing (witness cleaned it, disk issue), fall through
to create a new one.

**If no worktree** — create one scoped to the bead, not the agent:
```bash
WORKTREE_PATH=$(pwd)/worktrees/"$WORK_BEAD_ID"
git worktree add "$WORKTREE_PATH" --detach origin/{{base_branch}}
cd "$WORKTREE_PATH"
```
Record immediately so restarts and witness recovery can find it:
```bash
gc bd update "$WORK_BEAD_ID" --set-metadata work_dir="$WORKTREE_PATH"
```

**Whichever path you took**, make the worktree able to record hook errors
before you start working in it:
```bash
mkdir -p "$(pwd)/.remember/logs" "$(pwd)/.remember/tmp"
[ -e "$(pwd)/.remember/.gitignore" ] || printf '*\n' > "$(pwd)/.remember/.gitignore"
```
Your session runs in THIS worktree, so this is the directory the `remember`
plugin's hooks resolve against. The plugin registers its hooks with the stderr
redirect baked into the command string, and the shell opens that append target
before forking — so a missing `.remember/logs/` does not lose a log line, it
stops the hook from running at all. The hook it stops is the one that records
hook errors, so every hook failure goes silently missing. The plugin cannot
self-heal: the script that would create the directory is the one the missing
directory prevents from running. Keep this in sync with
`assets/scripts/remember-scaffold.sh`, which does the same job at pre_start for
agent workspaces.

Worktrees are scoped to the work bead (not the agent name) so that:
- An agent can pick up new work even if an old worktree is being recovered
- Multiple orphaned worktrees can coexist without collision
- The witness cleans them independently per-bead

**3. Ensure branch exists.**

Check if `metadata.branch` already records a branch:
```bash
BRANCH=$(gc bd show "$WORK_BEAD_ID" --json | jq -r '.[0].metadata.branch // empty')
```

**If branch exists in metadata** — treat it as authoritative. This
metadata may come from rejection recovery or from a caller that wants
work applied to an existing branch. Fetch the named remote branch first.
If `origin/$BRANCH` exists, make the local branch fast-forward to that
remote tip or stop on divergence:
```bash
git fetch origin "+refs/heads/$BRANCH:refs/remotes/origin/$BRANCH" || \
    echo "Could not fetch metadata.branch=$BRANCH from origin. Checking local refs before stopping."
if git show-ref --verify --quiet "refs/remotes/origin/$BRANCH"; then
    if git show-ref --verify --quiet "refs/heads/$BRANCH"; then
        git checkout "$BRANCH" || exit 1
        git merge --ff-only "origin/$BRANCH" || {
            echo "Local branch $BRANCH diverged from origin/$BRANCH."
            echo "STOP. Reconcile the branch before continuing."
            gc runtime drain-ack
            exit 1
        }
    else
        git checkout --track -b "$BRANCH" "origin/$BRANCH" || exit 1
    fi
elif git show-ref --verify --quiet "refs/heads/$BRANCH"; then
    git checkout "$BRANCH" || exit 1
else
    echo "metadata.branch=$BRANCH was set but no local or origin branch exists."
    echo "STOP. Do not create a different branch."
    gc runtime drain-ack
    exit 1
fi
```
If resuming a rejected branch, rebase onto latest base:
```bash
REJECTION=$(gc bd show "$WORK_BEAD_ID" --json | jq -r '.[0].metadata.rejection_reason // empty')
if [ -n "$REJECTION" ]; then
    git rebase origin/{{base_branch}}
    # If conflicts: resolve them (this is likely the rejection reason)
    # After resolving: git rebase --continue
    gc bd update "$WORK_BEAD_ID" \
      --unset-metadata rejection_reason \
      --set-metadata fork_sha="$(git rev-parse "origin/{{base_branch}}")"
fi
```

**If no branch** — create one and record it. Always branch from the
freshly-fetched `origin/{{base_branch}}`, never from local
{{base_branch}} or whatever branch happens to be checked out.
Polecat worktrees are reused across beads, so the local copy of
{{base_branch}} may lag the remote — branching from a stale local ref
inherits already-merged commits with stale hashes and causes the next
refinery rebase to reject for spurious "duplicate" conflicts.

```bash
git fetch origin {{base_branch}}                     # ensure origin ref is current
BRANCH="polecat/$WORK_BEAD_ID"
git branch -D "$BRANCH" 2>/dev/null || true          # drop any stale local branch with the same name
git checkout -B "$BRANCH" "origin/{{base_branch}}"   # force-create from the freshly-fetched remote tip
gc bd update "$WORK_BEAD_ID" \
  --set-metadata branch="$BRANCH" \
  --set-metadata fork_sha="$(git rev-parse "origin/{{base_branch}}")"
```

Recording the branch early means:
- Witness can find and salvage your work if you crash
- Rejection-aware resume knows which branch to check out
- The submit step updates the metadata (branch may change after rebase)

`fork_sha` is the base commit this branch was cut from. The refinery uses it to
tell a genuine already-merged branch (>=1 commit since fork) from a starved
0-commit branch (tip still at the fork point) when both look like ancestors of
the target.

**4. Ensure clean working state:**
```bash
git status                  # Should be clean
```

**5. Already-delivered gate — is the deliverable ALREADY on {{base_branch}}?**

Run this BEFORE `setup_command` and before writing a line of code. The bead
pool can go stale relative to the target branch: another machine, an earlier
polecat, or a human lands the work directly on origin/{{base_branch}} while the
bead describing it is still open in the pool. A polecat dispatched at that bead
takes a slot, builds a deliverable that is already present, produces a branch
with an empty diff, gets refused by the refinery's `branch_has_real_change()`
guard, and lands as a blocked bead on a human's desk. That guard works — but it
catches the waste at the END of the cycle, after the slot is spent.

```bash
gc gastown delivery-check "$WORK_BEAD_ID" --target "origin/{{base_branch}}"
echo "delivery check exit: $?"
```

Invoke it through `gc`, not by path. The check lives in the gastown pack, which
is materialized in a content-addressed cache, and `GC_PACK_DIR` is set only when
`gc` itself invokes a pack command — it is NOT part of your session environment.
A bare `"${GC_PACK_DIR:-}/assets/scripts/polecat-delivery-check.sh"` resolves to
`/assets/scripts/polecat-delivery-check.sh` in an agent session, fails its own
`-x` guard, prints "not resolvable", and moves on — permanently. That failure
reads exactly like a gate that ran and found nothing, which is the worst
available outcome for a check whose entire job is to fire rarely (see gp-fid).

The check gathers three independent signals against the target ref: a commit
whose message names this bead id (near-conclusive in a rig whose convention is
`<type>: <subject> (<bead-id>)`, and it needs no verify clause so it works on
every bead), a file path named by the bead's verify clause already existing,
and a code-shaped identifier from that clause already present in the content.

| verdict | exit | meaning | action |
|---------|------|---------|--------|
| `not-delivered` | 0 | evidence gathered, does not add up | BUILD. This is the common case. |
| `no-signal` | 0 | nothing checkable — the check ABSTAINED | BUILD. Abstention is not a clearance, and it is not a finding either. |
| `possibly-delivered` | 0 | every extracted token is already present | ADVISORY. Glance at the cited files, then BUILD. |
| `already-delivered` | 1 | a commit on the target names this bead | confirm, then halt — see below |
| — | 2 | bad ref, unreadable bead, no git | delivery was NOT evaluated. BUILD as normal; the refinery guard is still the backstop. Never read a 2 as a verdict in either direction. |

**Only `already-delivered` stops you, and only after you confirm it.** That
asymmetry is measured, not stylistic (gp-nrm): across 28 real beads the commit
signal caught 8/8 already-delivered with zero false positives on 14 open beads,
while the token heuristic behind `possibly-delivered` fired zero times. A signal
that has never fired does not get to halt a polecat — a miss costs one pool slot
and the refinery still catches it, but a false positive silently drops real work.

**A verdict is NEVER sufficient on its own.** Open the cited commit or file and
read it:

```bash
git show <cited-sha> --stat        # does this commit actually deliver the bead?
git show origin/{{base_branch}}:<cited-path> | head -40
```

Ask whether the deliverable the bead asks for is *genuinely there* — not
merely that its neighbourhood exists. A test file named by a verify clause
routinely predates the fix the bead wants; a doc that mentions the subject is
not the same as a doc that documents it. **If confirmation is anything short of
convincing, BUILD.** A false already-delivered silently drops real work, which
is strictly worse than the wasted slot this gate exists to save.

**If you confirm it is already delivered:** stop here. Do not build, do not
push an empty branch, and do NOT hand off to the refinery — there is nothing to
merge, and an empty branch is exactly the false-completion the refinery would
refuse. Report the evidence instead, and clear the pool routing so no other
polecat repeats the cycle:

```bash
gc bd update "$WORK_BEAD_ID" \
  --status=blocked \
  --assignee="" \
  --set-metadata gc.routed_to=human \
  --set-metadata already_delivered_suspected="<verdict> vs origin/{{base_branch}}: <sha or path cited by the check>" \
  --notes "Already delivered before build: <what you confirmed, and where>.
Evidence: <sha> <subject> / <path>. No branch pushed — nothing to merge.
Verified by reading the commit, not by the check's verdict alone."
gc session nudge "${GC_RIG:+$GC_RIG/}{{binding_prefix}}witness" "ALREADY-DELIVERED: $WORK_BEAD_ID — deliverable confirmed present on origin/{{base_branch}} (<sha>); halted before build, no branch pushed."
gc runtime drain-ack
exit 0
```

`blocked` + `gc.routed_to=human` is deliberate: the bead needs one cheap human
call ("close it as done, or re-scope it") and must not fall back into the
polecat pool, which would re-run this whole cycle. Unlike the refinery's
post-hoc `refused_false_completion` — a negative inference with nothing to act
on — this carries the positive evidence, so the call takes seconds instead of
an investigation.

**6. Run project setup (if configured):**
```bash
{{setup_command}}
```
Empty setup_command → skip.

**Exit criteria:** In your worktree, on a clean feature branch, rebased
on latest {{base_branch}}, the already-delivered gate cleared (or halted
with evidence), deps installed, worktree and branch recorded on the bead."""

[[steps]]
id = "self-review"
title = "Self-review and run tests (affected-aware)"
needs = ["implement"]
description = """
Review your changes and verify they work. Override of mol-polecat-base
`self-review` to support affected-test detection — when the rig configures
`affected_tests_command`, only the tests touched by your diff run, instead
of the full suite. Saves polecat time and reduces CI clog from PRs whose
locally-runnable failures should have been caught before push.

**Config: setup_command = {{setup_command}}**
**Config: typecheck_command = {{typecheck_command}}**
**Config: lint_command = {{lint_command}}**
**Config: build_command = {{build_command}}**
**Config: test_command = {{test_command}}**
**Config: affected_tests_command = {{affected_tests_command}}**

**1. Review the diff:**
```bash
git diff origin/{{base_branch}}...HEAD
git log --oneline origin/{{base_branch}}..HEAD
git diff --stat origin/{{base_branch}}...HEAD
```

Check for: bugs, security issues, style violations, missing error handling,
debug cruft, unintended file changes. Fix anything you find.

**2. Run quality checks (skip empty commands):**
```bash
{{setup_command}}
{{typecheck_command}}
{{lint_command}}
{{build_command}}
```

**3. Run tests — affected subset if rig provides one, full suite otherwise:**
```bash
if [ -n "{{affected_tests_command}}" ]; then
    # Rig has an affected-tests detector. Run only the tests our diff
    # touches — same logic the rig's CI uses to dispatch test workflows.
    # The command must read `git diff --name-only origin/{{base_branch}}...HEAD`
    # internally and execute the relevant test subset.
    {{affected_tests_command}}
else
    # No affected detector — run the full suite (legacy behavior).
    {{test_command}}
fi
```

**ALL CHECKS MUST PASS.** If your change caused the failure, fix it.
If pre-existing, file a bead.

Refusing to push on local failure is a feature: the rig's CI will run the
same affected subset (and more), and a PR that already fails locally will
fail in CI too — wasting reviewer attention and CI minutes. Fix before
submitting.

**4. Ensure everything is committed:**
```bash
git status                  # Must be clean
git log origin/{{base_branch}}..HEAD --oneline  # Must show your commits
```

If uncommitted changes exist, commit them with a body explaining what
slipped through the `implement` step (typically a self-review touch-up,
test addition, or formatter run):
```bash
CONVOY_STATUS=$(gc convoy status {{convoy_id}} --json)
WORK_BEAD_ID=$(printf '%s' "$CONVOY_STATUS" | jq -r 'if (.children | length) == 1 then .children[0].id else empty end')
git add -A
git commit -m "<type>: <imperative subject> ($WORK_BEAD_ID)" -m "$(cat <<'BODY'
<Explain what these residual changes are: a self-review fix,
test-only addition, formatter run, etc. Be specific.>
BODY
)"
```

NEVER discard implementation changes with `git checkout -- .`

**Exit criteria:** All checks pass (affected subset OR full suite), all
changes committed, working tree clean."""

[[steps]]
id = "submit-and-exit"
title = "Submit work to refinery and exit"
needs = ["self-review"]
description = """
Hand off your work and self-clean. You cease to exist after this step.

**1. Branch-shape gate (fails closed — non-negotiable).**

Before anything else, verify you are on a per-bead branch. The refinery
merges the branch recorded in your bead's `metadata.branch`, which must
follow the `polecat/<bead-id>` convention; if you push from a different
branch (your agent home branch, a stray local checkout, etc.), the
handoff contract is broken and the bead is permanently stranded as
"assigned to refinery, no merge target". See gastownhall/gascity#2082.

```bash
CONVOY_STATUS=$(gc convoy status {{convoy_id}} --json)
WORK_BEAD_ID=$(printf '%s' "$CONVOY_STATUS" | jq -r 'if (.children | length) == 1 then .children[0].id else empty end')
CURRENT_BRANCH=$(git branch --show-current)
EXPECTED_BRANCH="polecat/$WORK_BEAD_ID"
if [ "$CURRENT_BRANCH" != "$EXPECTED_BRANCH" ]; then
    echo "BRANCH SHAPE GATE FAILED"
    echo "  current branch:  $CURRENT_BRANCH"
    echo "  expected branch: $EXPECTED_BRANCH"
    echo ""
    echo "Refinery handoff merges metadata.branch, which must be 'polecat/<bead-id>'."
    echo "Pushing this commit and reassigning to refinery would strand the work."
    echo ""
    echo "Recover by re-running the workspace-setup step:"
    echo "  - Create the per-bead worktree at \\$(pwd)/worktrees/$WORK_BEAD_ID"
    echo "  - Create branch '$EXPECTED_BRANCH' from origin/{{base_branch}}"
    echo "  - Cherry-pick your commits from $CURRENT_BRANCH onto the new branch"
    echo "  - Re-run submit-and-exit"
    echo ""
    echo "Do NOT proceed past this gate."
    gc runtime drain-ack
    exit 1
fi
```

Also confirm `metadata.branch` reflects this branch (workspace-setup
recorded it, but a divergent local state would be invisible to the
refinery without this re-check):
```bash
METADATA_BRANCH=$(gc bd show "$WORK_BEAD_ID" --json | jq -r '.[0].metadata.branch // empty')
if [ "$METADATA_BRANCH" != "$EXPECTED_BRANCH" ]; then
    gc bd update "$WORK_BEAD_ID" --set-metadata branch="$EXPECTED_BRANCH"
fi
```

**2. Final clean-state verification (safeguard):**
```bash
git status --porcelain
```
If ANY output (untracked files, uncommitted changes):
```bash
git add -A
git commit -m "chore: capture remaining work ($WORK_BEAD_ID)" -m "$(cat <<'BODY'
<Explain the remaining changes discovered by final clean-state
verification and why they are safe to hand off.>
BODY
)"
```
This is a belt-and-suspenders check — self-review should have caught this,
but we never push with untracked work left behind.

**2b. No-patch gate — does this bead even have a patch to hand off?**

`metadata.no_code_change=true` marks a bead whose deliverable is not a patch: a
validation task that records verdicts in an external tracker, a state correction
in another system, an investigation whose product is the note it writes. The
work is real; it just does not land as a diff.

Such a bead must not be routed into the refinery's MERGE queue. A merge queue
asserts exactly one thing — "this branch landed on the target" — and for a
zero-diff branch that is unprovable, so the refinery's `branch_has_real_change()`
guard refuses to close it and halts. **That halt is correct and is not being
weakened**: refusing to close a zero-diff bead as merged is precisely what
catches a polecat that died having produced nothing. The defect is that
non-patch work was handed to a patch-merger at all (gp-d5u, first observed on
ta-af7).

Your job here is narrow. Make the flag TRUE OR FALSE about what you actually
did, and attach the evidence. The refinery owns the routing and the close.

```bash
NO_CODE_CHANGE=$(gc bd show "$WORK_BEAD_ID" --json \
  | jq -r '.[0].metadata | if has("no_code_change") then (.no_code_change | tostring) else "" end')
COMMITS_AHEAD=$(git rev-list --count "origin/{{base_branch}}..HEAD" 2>/dev/null) || COMMITS_AHEAD=""
```

Read `no_code_change` through `tostring`. `gc bd update --set-metadata k=true`
stores a JSON **boolean**, so a comparison against the string `"true"` inside
jq silently never matches — the filter reads as present and does nothing.

An empty `COMMITS_AHEAD` means the count did not evaluate. Do NOT guess in
either direction: `git fetch origin {{base_branch}}` and retry. If it still will
not answer, leave the flag untouched and hand off normally — the refinery
re-checks this independently and fails closed.

**Flag set, but you produced commits** → the flag is wrong for this branch.
Clear it. Real code must never be dropped on the strength of a marker claiming
it would not exist:
```bash
if [ "$NO_CODE_CHANGE" = "true" ] && [ -n "$COMMITS_AHEAD" ] && [ "$COMMITS_AHEAD" -gt 0 ]; then
  gc bd update "$WORK_BEAD_ID" \
    --unset-metadata no_code_change \
    --notes "no_code_change=true was set on this bead, but the branch carries $COMMITS_AHEAD commit(s). Cleared the flag: there IS a patch and it must go through the merge queue."
  NO_CODE_CHANGE=""
fi
```

**Flag set and the branch is genuinely empty** → record what you delivered and
where you verified it. This note is the entire reason the refinery's no-patch
path can be a seconds-long human call instead of an investigation, so write it
for a reader who has none of your context:
```bash
if [ "$NO_CODE_CHANGE" = "true" ] && [ "$COMMITS_AHEAD" = "0" ]; then
  gc bd update "$WORK_BEAD_ID" \
    --set-metadata no_code_change_evidence="<system>: <what you recorded or changed there>; verified by <the check you ran at the source>" \
    --notes "NO PATCH BY DESIGN — nothing to merge. Branch $EXPECTED_BRANCH is 0 commits ahead of origin/{{base_branch}}; the empty diff is the intended outcome, not a failure.
DELIVERABLE: <what the bead asked for, and where it now lives — the system, the ids, the values>.
VERIFIED: <the independent check you ran at the source, and what it returned>.
Refinery: there is no merge to assert. Confirm the deliverable at the source, then close as done-without-merge."
fi
```

Then continue to the push below. **Push the empty branch anyway — do not skip
it.** The pushed ref is the positive evidence that you ran to completion and
deliberately produced no patch. Its absence is indistinguishable from a polecat
that died before pushing, and removing that ambiguity is the whole point of this
path.

**Never set `no_code_change` yourself to escape a build you could not finish.**
You cannot prove to anyone when the flag was set — `gc bd history` versions the
issue row but NOT its metadata — so downstream it is treated as a claim
requiring evidence, never as a clearance, and it never auto-closes a bead. A
polecat that flags its way out of unfinished work is producing a false
completion by a quieter route than the one the halt catches.

**3. Push your branch:**
```bash
AUTO_PUSH=$(gc bd show "$WORK_BEAD_ID" --json | jq -r '.[0].metadata | if has("auto_push") then (.auto_push | tostring) else "" end')
if [ "$AUTO_PUSH" = "false" ]; then
  echo "auto_push=false: halting at branch-ready (no push, no refinery handoff)"
  BRANCH=$(git branch --show-current)
  gc bd update "$WORK_BEAD_ID" \
    --status=open --assignee="" \
    --set-metadata branch="$BRANCH" \
    --set-metadata target={{base_branch}} \
    --set-metadata branch_ready=true \
    --set-metadata halt_reason=auto_push_false \
    --set-metadata gc.routed_to="" \
    --notes "Branch ready: auto_push=false (no push, no refinery handoff)"
  gc runtime drain-ack
  exit 0
fi
git push origin HEAD
PUSH_EXIT=$?
if [ $PUSH_EXIT -ne 0 ]; then
    echo "PUSH FAILED (exit $PUSH_EXIT)"
    echo "Branch '$CURRENT_BRANCH' was NOT pushed to origin."
    echo "Aborting handoff — bead stays with polecat."
    echo ""
    echo "Diagnose the push failure (network, auth, force-needed?) and re-run submit-and-exit."
    gc runtime drain-ack
    exit 1
fi
# Verify the ref is reachable on origin before trusting the push succeeded.
REMOTE_REF=$(git ls-remote origin "refs/heads/$CURRENT_BRANCH" 2>/dev/null | awk '{print $1}')
LOCAL_HEAD=$(git rev-parse HEAD)
if [ -z "$REMOTE_REF" ]; then
    echo "PUSH VERIFICATION FAILED"
    echo "  git push exited 0 but 'git ls-remote' returned no ref for '$CURRENT_BRANCH' on origin."
    echo "  Branch is local-only. Aborting handoff."
    gc runtime drain-ack
    exit 1
fi
if [ "$REMOTE_REF" != "$LOCAL_HEAD" ]; then
    echo "PUSH VERIFICATION FAILED"
    echo "  origin/$CURRENT_BRANCH = $REMOTE_REF"
    echo "  local HEAD             = $LOCAL_HEAD"
    echo "  Branch tip on origin does not match local HEAD. Aborting handoff."
    gc runtime drain-ack
    exit 1
fi
```

**4. Clean up local branch (prevent stale branch accumulation):**
```bash
BRANCH=$(git branch --show-current)
git checkout --detach                 # Detach so we can delete the branch
git branch -D "$BRANCH"              # Branch is pushed; refinery owns it now
```

**5. Update metadata on the work bead:**
```bash
gc bd update "$WORK_BEAD_ID" \
  --set-metadata target={{base_branch}} \
  --notes "Implemented: <brief summary>"
```
Branch was recorded in workspace-setup and is already in metadata.
This adds the target for the refinery. If an existing pull request was
provided by the caller, `metadata.existing_pr` is preserved for refinery
validation. The refinery records canonical `pr_url` only after verifying
the PR is open and matches the branch, base, and origin repository.

**5b. Stamp the token spend for this bead:**

Your spend is already metered — the runtime writes a row per model call to
`<city>/.gc/usage.jsonl`, keyed by your session. What is missing is the join
to the bead you spent it on, and this is the last moment it is unambiguous:
the session is yours, the work is done, and nobody else has touched the bead.

```bash
gc gastown usage-stamp "$WORK_BEAD_ID" || true   # never gate the handoff on metering
```

Invoke it through `gc`, not by path. The join lives in the gastown pack, which
is materialized in a content-addressed cache, and `GC_PACK_DIR` is set only
when `gc` itself invokes a pack command — it is NOT part of your session
environment. A bare `"${GC_PACK_DIR:-}/assets/scripts/usage-stamp.sh"` resolves
to `/assets/scripts/usage-stamp.sh` in an agent session, silently stamps
nothing, and reads like it ran.

Do NOT report token counts yourself. You cannot read your own usage
accurately; the runtime records it exactly. Measurement belongs to whatever
can measure.

Exit 0 stamps `usage.model` plus the four token totals under the `usage.`
prefix. Exit 1 means unmetered — it records `usage.status=unmetered` with a
reason and writes no token fields, because a bead claiming 0 in / 0 out is
indistinguishable from work that was genuinely free. Neither outcome blocks
the handoff: metering is an observation, never a gate on work changing hands.

**6. Reassign to refinery:**
```bash
REFINERY_TARGET="${GC_RIG:+$GC_RIG/}{{binding_prefix}}refinery"
gc bd update "$WORK_BEAD_ID" --status=open --assignee="$REFINERY_TARGET" --set-metadata gc.routed_to=""
```

`${GC_RIG:+$GC_RIG/}{{binding_prefix}}refinery` resolves to
`$GC_RIG/{{binding_prefix}}refinery` when running inside a rig session,
or `{{binding_prefix}}refinery` when running in an HQ-only city where
the workspace is the rig. Writing a rendered empty rig prefix produces
`/{{binding_prefix}}refinery` and strands beads outside the refinery pool.

Set `assignee` to the configured refinery named-session identity and clear
`gc.routed_to` because the direct assignment is the handoff. Generic pool
demand is represented by unassigned work with `gc.routed_to=<target>`, so
named-session work should not retain pool-demand metadata.

The refinery will pick this up, rebase onto {{base_branch}}, run tests,
merge, and close the bead. If there's a conflict, the refinery puts the
bead back in the pool with `rejection_reason` metadata — a new polecat
picks it up and resumes from the existing branch.

If `no_code_change` survived step 2b, the refinery routes the bead to its
no-patch triage instead of the merge queue: it re-confirms the empty diff itself,
then blocks the bead and routes it to a human for a done-without-merge close,
carrying your `no_code_change_evidence`. Reassigning to the refinery is still
correct — it remains the only agent that closes beads — you are simply handing
it a bead it will terminate a different way. Do NOT close it yourself.

**7. Signal refinery to check for work immediately (belt-and-suspenders):**
```bash
REFINERY_TARGET="${GC_RIG:+$GC_RIG/}{{binding_prefix}}refinery"
gc session wake "$REFINERY_TARGET" || true
gc session nudge "$REFINERY_TARGET" "Run 'gc prime' to check merge queue and begin processing." || true
```

The `gc bd update` in step 6 generates a `bead.updated` event the refinery's
event-watch will see, but that can take up to `event_timeout` seconds. Wake
starts a stopped on_demand session; nudge leaves a visible prompt for a running
one as soon as it is ready for input. Failure is non-fatal: the refinery finds
the work on its next poll.

**8. Signal reconciler and exit.**
```bash
gc runtime drain-ack
exit
```

`gc runtime drain-ack` tells the reconciler to kill this session. The
reconciler only restarts you if the pool check command finds more work.
You are GONE. Done means gone. There is no idle state.

**Exit criteria:** Branch pushed, metadata set, bead reassigned, refinery signaled, drain acknowledged, session exited."""
