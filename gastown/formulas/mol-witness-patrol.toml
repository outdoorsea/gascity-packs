# CONTRACT PIN — read before editing this file.
#
# The gastown-orchestration gate pins literal text fragments from this formula
# in GASTOWN_FORMULA_CONTRACTS / GASTOWN_BUILD_WORKFLOW_CONTRACTS
# (scripts/gascity_pack_inference_gate.py). Nothing below marks which lines they
# are, so list them before you edit:
#
#   python3 scripts/gascity_pack_inference_gate.py --list-formula-contracts mol-witness-patrol
#
# Dropping a pinned fragment fails the gate and gets the branch rejected. If the
# contract genuinely changed, update the registry in the SAME commit — never
# delete a pin just to go green.
description = """
Witness patrol loop. Poured as a root-only wisp on startup:

  gc gastown wisp-reconcile ensure mol-witness-patrol --var binding_prefix='{{binding_prefix}}'

That command pours AND assigns, and pours only if the agent does not
already own a wisp. Never bootstrap with a bare `gc bd mol wisp` — an
unguarded pour leaks a wisp on every restart.

Each wisp is ONE iteration: check for work, patrol, pour the next
iteration. On crash, re-read the formula steps and determine where
you left off from context (bead state, mail state, last action).

Formula steps are NOT materialized as child beads. Read the step
descriptions below and work through them in order.

The loop mechanism: EVERY step is one leg of the same wisp. Never exit
the wisp from an intermediate step — either continue to the next step
or jump directly to `next-iteration` to pour and burn. Exiting without
burning leaks wisps (see #1884). The prompt only bootstraps the first
wisp.

## Witness Role

The witness is the rig's work-health monitor. It does NOT manage processes
(the controller handles start/stop/restart/zombie detection). The witness
monitors the WORK layer:

1. **Orphaned bead recovery** — beads assigned to agents that won't spawn
   (pool max changed, agent removed from config). This is the core job.
2. **Refinery queue health** — work beads assigned to refinery, staleness.
3. **Parked sessions** — sessions stopped at a provider usage-limit prompt.
   They report `active` to everything else, so nothing else will catch them.
4. **Polecat health** — detect stuck polecats, file warrants for dog pool.
5. **Help mail** — triage HELP/escalation requests from polecats.
6. **Landed-worktree reaping** — per-bead polecat worktrees whose work already
   reached the target branch. Nothing else owns their death: the refinery
   deletes the BRANCH after merge, not the tree, and orphan recovery keys on a
   live-but-unreachable assignee, which a closed bead never is.

Gate checks and convoy/swarm completion are town-wide concerns handled by
the deacon, not the per-rig witness.

## Canonical Work Chain

```
worktree → (push) → branch → (merge) → target branch
```

Each transition moves the canonical location of the work. Once moved,
the previous location is disposable:
- After push: worktree disposable (branch is canonical)
- After merge: branch disposable (target is canonical)

The witness's core recovery job: when a bead is orphaned (agent won't
come back), ensure the work reaches the branch (push), then clean up
the worktree. This makes the work schedulable again.

The same chain licenses the reaper in `reap-landed-worktrees`: once a bead is
closed and its commits' patches are on the target, BOTH earlier locations are
disposable, and the tree is pure garbage. That step is the only place the chain
is read forwards (work landed, so drop the tree) rather than backwards (work is
stranded, so push it).

## What the witness does NOT do

- Zombie detection (controller reconcile loop handles this)
- Process start/stop (controller handles this)
- Code implementation (polecats do this)
- Gate checks (deacon handles town-wide)
- Convoy/swarm completion (deacon handles cross-rig)
- Kill stuck agents directly (files warrant, dog pool runs shutdown dance)

Read each step's description before acting — Config values override defaults."""
formula = "mol-witness-patrol"

[vars]
[vars.binding_prefix]
description = "Import binding prefix for gastown agent identities, including trailing dot when bound."
default = ""

[vars.event_timeout]
description = "Seconds to sleep before re-checking. Replaces former event-watch loop which hot-spun on cache-reconcile firehose."
default = "60"

[vars.parked_settle_secs]
description = "Seconds between the two pane peeks that decide whether a session showing a usage-limit banner is parked or still advancing. Paid once for the whole rig, and only when some session is actually showing a banner — the peek pass itself costs ~17s for a 5-session rig (measured, concurrent peeks in batches of 8). Long enough that an auto-retry or a re-render lands inside the window and reports `settling` instead of `parked`; short enough that a genuinely parked session is recovered within one cycle rather than sitting on a P1 bead for hours."
default = "45"

[vars.polecat_stale_min]
description = "Minutes of polecat silence — no file write under the worktree, no bead update — before check-polecat-health's deterministic pass marks the bead stale. Stale is a candidate for a proof-of-life peek, never a warrant on its own. The default leaves an order of magnitude over the longest common legitimate silence (one build/vet/test cycle inside a single tool call, ~4 minutes on this fleet) while still surfacing a genuine multi-hour wedge within one patrol cycle. Raise it for rigs whose test suites run long."
default = "45"

[[steps]]
id = "check-inbox"
title = "Context check, then mail"
description = """
**1. Context check (FIRST — before any work):**
```bash
RSS=$(ps -o rss= -p $$ | tr -d ' ')
RSS_MB=$((RSS / 1024))
```

If RSS > 1500 MB or context feels heavy, request a restart:
```bash
gc runtime request-restart
```
This sets `GC_RESTART_REQUESTED` metadata on the session and blocks
forever. The controller will kill and restart the session on the next
reconcile tick. The current wisp stays assigned and the new session
the new session re-reads formula steps and resumes from context.

**2. Check mail:**
```bash
gc mail inbox
```

Handle each message type:

**HELP / Blocked:**
Classify by keyword, then act based on severity:

| Category | Keywords | Severity | Action |
|----------|----------|----------|--------|
| Emergency | crash, data loss, corruption, security | Critical | Mail mayor immediately [HIGH] |
| Failed | failed, error, cannot, broken, exception | High | Investigate; mail mayor if unresolvable |
| Blocked | blocked, waiting, depends on, need access | Medium | Try to unblock (point to docs, suggest approach); escalate if you can't |
| Decision | should I, which approach, trade-off, design | Low | Answer if you can; escalate only if architectural |
| Lifecycle | done, finished, idle, restarting | Info | Acknowledge; no escalation needed |
| General help | help, stuck, confused, how do I | Low | Point to docs/suggest approach; escalate only if truly stuck |

For escalations:
```bash
gc mail send mayor/ -s "ESCALATION: <polecat> needs help [HIGH|MED|LOW]" -m "<category>: <details>"
```
Do NOT escalate routine blocks or lifecycle messages — handle them yourself.
Archive after handling.

**HANDOFF:**
Read predecessor context. Continue from where they left off.
Archive after absorbing context.

**Other / informational:**
Archive after reading.

**Hygiene principle**: Archive messages after they're fully processed.
Inbox should be near-empty after this step.

Close this step and continue to `recover-orphaned-beads` — do NOT exit
the wisp here. The `next-iteration` step is the only place that pours
the next wisp and burns this one."""

[[steps]]
id = "recover-orphaned-beads"
title = "Recover orphaned work beads"
needs = ["check-inbox"]
description = """
**This is the witness's core job.**

Find beads assigned to agents that will never process them, salvage any
unpublished work from the worktree, and return beads to the pool.

This happens when:
- Pool max was reduced (agents that won't spawn anymore)
- An agent was removed from config
- An agent crashed and the controller decided not to restart it (quarantine)

**Step 1: Find orphaned beads via session-ID liveness.**

List beads assigned to agents in YOUR rig:
```bash
gc bd list --status=in_progress --json --limit=0
gc bd list --status=open --json --limit=0
```

Filter for beads with an assignee (skip unassigned beads). This pass does not
solve the controller race where orphaned work may already have been released to
open/unassigned before witness observes it; that requires a separate
worktree-salvage scan keyed by `metadata.work_dir`. Bound Gastown polecat
assignees use the shape `<rig>/{{binding_prefix}}<polecat-suffix>`.

Get the full session roster for liveness checks:
```bash
gc session list --state=all --json
gc bd list --type=session --label=gc:session --include-infra --include-gates --all --json --limit=0
```

For each bead with an assignee, check **session-ID liveness** — NOT
template pattern matching. Pool instances get new IDs on restart, so
matching against a template pattern (e.g., `worker-3` matches template
`worker`) gives false negatives: the old session ID is dead but the
witness skips it thinking "the pool will restart it." The pool creates
a NEW session with a different ID — the old one never comes back.

**Liveness check procedure:**

1. Build the assignee→state liveness map ONCE per cycle (before the
   per-bead loop), then resolve each bead's assignee against it. Resolve by
   exact identifier lookup — do NOT extract a session ID with a regex or
   fixed prefix list: rig prefixes are configuration-derived, and assignees
   may be a session bead ID, session name, alias, concrete agent name, or
   configured named identity.

   Two schema facts this recipe depends on (verified live, gc-3tn8g):
   - `gc session list --json` returns an OBJECT `{sessions:[...], ...}`, not
     a top-level array — iterate `.sessions[]`, never the top level (the
     top-level values include booleans, so `$x.id` on them errors).
   - Session fields are lowercase snake_case: `id / name / session_name /
     alias / agent_name / state / closed`. Key the map on `id`, `name`,
     `session_name`, `alias`, and `agent_name` — the exact identifier forms
     an assignee may take.

   Stream both JSON blobs through temp files, never `--argjson` on argv: on a
   busy city the session `command` fields are large and overflow ARG_MAX
   (`argument list too long: jq`). Build the map once:
   ```bash
   SESSIONS_FILE=$(mktemp); SESSION_BEADS_FILE=$(mktemp)
   gc session list --state=all --json > "$SESSIONS_FILE"
   gc bd list --type=session --label=gc:session --include-infra --include-gates --all --json --limit=0 > "$SESSION_BEADS_FILE"

   LIVENESS_MAP=$(jq -n --slurpfile sessions "$SESSIONS_FILE" --slurpfile session_beads "$SESSION_BEADS_FILE" '
     def add($m; $key; $state; $closed):
       if (($key // "") | length) == 0 then $m
       else $m + {($key): (if $closed then "closed" else ($state // "") end)}
       end;
     (reduce ($sessions[0].sessions // [])[] as $s ({};
         add(.; $s.id;         $s.state; ($s.closed // false))
       | add(.; $s.name;       $s.state; ($s.closed // false))
       | add(.; $s.session_name; $s.state; ($s.closed // false))
       | add(.; $s.alias;      $s.state; ($s.closed // false))
       | add(.; $s.agent_name; $s.state; ($s.closed // false))
     )) as $from_sessions
     | reduce ($session_beads[0] // [])[] as $b ($from_sessions;
         add(.; $b.metadata.configured_named_identity; $b.metadata.state; ($b.status == "closed")))')

   SESSION_COUNT=$(jq -r '(.sessions // []) | length' "$SESSIONS_FILE")
   MAP_COUNT=$(printf '%s' "$LIVENESS_MAP" | jq -r 'length')
   rm -f "$SESSIONS_FILE" "$SESSION_BEADS_FILE"
   ```

   **Fail safe — never orphan on an empty map.** If the map is empty while
   sessions exist, the schema has drifted again (the exact failure in
   gc-3tn8g). An empty map resolves every assignee to `absent` and would
   false-orphan live agents. ABORT recovery this cycle — do NOT classify any
   bead as orphaned — escalate, then continue to `check-refinery`:
   ```bash
   if [ "${MAP_COUNT:-0}" -eq 0 ] && [ "${SESSION_COUNT:-0}" -gt 0 ]; then
     echo "FAIL-SAFE: empty liveness map with $SESSION_COUNT live sessions — skipping orphan recovery (treat all assignees as live)."
     gc mail send mayor/ -s "WITNESS: orphan-recovery disabled — empty liveness map" -m "Built an empty assignee->state map from $SESSION_COUNT live sessions. gc session list --json schema likely drifted again (gc-3tn8g); skipping orphan recovery this cycle to avoid false-orphaning live agents. Re-check the recover-orphaned-beads liveness recipe."
     # Skip the rest of this step; proceed to check-refinery.
   fi
   ```

   Resolve each bead's assignee against the prebuilt map:
   ```bash
   STATE=$(printf '%s' "$LIVENESS_MAP" | jq -r --arg a "$ASSIGNEE" '.[$a] // "absent"')
   ```

2. An `absent` lookup (assignee not in the map) is orphaned only for
   pool/ephemeral work identities. If the assignee is a refinery, witness, or
   other configured infrastructure identity, skip it instead of recovering it.

3. Classify:
   - `active` or `awake` → **not orphaned** (may be stuck —
     `check-polecat-health` handles that separately)
   - `creating`, `asleep`, `drained`, `suspended`, `draining`, or
     `quarantined` → **not orphaned** (controller or operator state still owns
     the session)
   - `archived`, `closed`, or `absent` after the exact lookup → **orphaned
     bead** for pool/ephemeral work — the owning session is gone and will never
     come back, regardless of whether the pool template still exists

**Important**: Beads assigned to the refinery, witness, or other
infrastructure agents (not pool instances) should be skipped — those
agents have persistent identity and the controller manages their
lifecycle directly.

**Step 2: For each orphaned bead, salvage work from the worktree.**

The canonical work chain: `worktree → (push) → branch → (merge) → target`.
Our job is to ensure work reaches the branch so it's schedulable.

Read the bead metadata to find the worktree and branch:
```bash
META=$(gc bd show <bead> --json | jq '.[0].metadata')
WORKTREE=$(echo "$META" | jq -r '.work_dir // empty')
BRANCH=$(echo "$META" | jq -r '.branch // empty')
```

`work_dir` is the key the polecat actually writes in its branch-setup step —
not `worktree`, and not the short form `gc.work_dir` (which resolves to the rig
root, not the bead's worktree). Reading the wrong key yields an empty
`$WORKTREE`, which silently routes every orphan to Case E and discards
unpushed work.

Check the bead and worktree state, then act:

**Case A: Branch exists on origin (work already published).**
Worktree is disposable — canonical work is on the branch.
```bash
git ls-remote --heads origin $BRANCH  # Verify branch exists on remote
```
If branch exists on origin → skip to Step 3 (cleanup).

**Case B: Worktree exists, branch pushed but not on origin yet.**
Branch is recorded in metadata but polecat may have crashed before push.
```bash
cd "$WORKTREE"
git ls-remote --heads origin $BRANCH
```
If branch IS on origin → skip to Step 3.
If branch is NOT on origin → fall through to Case C.

**Case C: Worktree exists with unpushed commits.**
Work is committed locally but never pushed. Branch is not yet canonical.
```bash
cd "$WORKTREE"
BRANCH=$(git branch --show-current)
git log origin/main..HEAD --oneline  # Shows unpushed commits
```
Commit any remaining uncommitted/untracked work first (safeguard):
```bash
git status --porcelain
# If any output:
git add -A
git commit -m "witness: salvage uncommitted work (<bead>)"
```
Publish the work to make branch canonical:
```bash
git push origin HEAD
gc bd update <bead> --set-metadata branch=$BRANCH
```
Skip to Step 3 (cleanup).

**Case D: Worktree exists with ONLY uncommitted/untracked changes (no commits).**
Same resolution as Case C. All work is useful work — never discard.
```bash
cd "$WORKTREE"
git add -A
git commit -m "witness: salvage work from orphaned agent (<bead>)"
BRANCH=$(git branch --show-current)
git push origin HEAD
gc bd update <bead> --set-metadata branch=$BRANCH
```
Skip to Step 3 (cleanup).

**Case E: No working directory exists in EITHER scope, no branch on origin.**
Nothing to salvage. The bead goes back to pool and a new polecat
starts fresh.

"No working directory" means both keys were checked and neither names a
directory that is present on disk:

```bash
gc bd show "$BEAD" --json | jq -r '
  .[0].metadata
  | [ (.work_dir // empty), (."gc.work_dir" // empty) ]
  | map(select(. != "")) | unique | .[]'
```

A missing `metadata.work_dir` alone does NOT put a bead in Case E. That key is
absent for every polecat that halted before its branch-setup step, and those are
exactly the sessions whose uncommitted work sits in the `gc.work_dir` agent home.
Classifying them as "nothing to salvage" on the unprefixed key alone is how live
unpushed work gets discarded (gp-6k8).

**The two scopes are probed the same way but salvaged differently.** Cases A-D
resolve their worktree from `metadata.work_dir` only, and that does not change:
the agent home sits on the agent's own `gc-<agent>-<sha>` branch, so committing
bead work there and pushing would publish the agent home branch instead of
`polecat/<bead>` and strand the bead with no merge target. Never salvage by
pushing from the agent home.

So when the per-bead worktree is absent and only the agent home exists:

1. Probe it READ-ONLY (`status --porcelain`, `log origin/main..HEAD`,
   `stash list`) — evidence only, no commits, no pushes.
2. All three empty -> Case E genuinely applies. Reset the bead to pool.
3. Anything non-empty -> there is unpublished work and no branch to put it on.
   Do NOT reset and do NOT push. Leave the bead and escalate with the evidence:

```bash
gc mail send mayor/ -s "WITNESS: orphaned bead with work in the agent home" -m "Bead: <bead>
Agent home: <gc.work_dir> (branch $(git -C "<gc.work_dir>" branch --show-current))
Dirty paths / unpushed commits / stashes: <what you found>
No per-bead worktree or branch exists, so there is nothing to push this to
without inventing one. Requesting a decision."
```

**Step 3: Verify work on main before resetting.**

Before resetting the bead to pool, check if the branch was already merged
to main (polecat finished, pushed, but crashed before closing the bead):
```bash
git log main --oneline | grep -q "$BRANCH"
# Or check if branch commits are reachable from main:
git merge-base --is-ancestor origin/$BRANCH origin/main
```

If the work is already on main:
- **Close the bead** (work is done, don't re-dispatch). Use `--force`: the bead
  is still assigned to the crashed polecat, so this is a cross-actor close and
  bd's close-authority guard (gastownhall/beads#3734) would otherwise refuse it:
```bash
gc bd close <bead> --force
gc mail send mayor/ -s "ORPHAN_CLOSED: <bead> already merged" \
  -m "Branch $BRANCH already on main. Closed instead of resetting."
```
- Clean up worktree and skip to Step 4.

If the work is NOT on main, proceed with reset:

**Step 3b: Clean up worktree and return bead to pool.**

Once the branch is canonical (on origin), the worktree is disposable:
```bash
# Delete worktree if it exists
cd <rig-root>
git worktree remove <worktree-path> --force 2>/dev/null
rm -rf <worktree-path> 2>/dev/null  # Fallback
git worktree prune
```

Close any orphaned workflow subtree and return the bead to pool with one
command:
```bash
gc gastown reopen-source <bead>
```

That is the whole recovery. It closes the workflow subtree, reopens and
unassigns, parks the dead round's disposition and claim identity under
`round<N>.*`, and sets `gc.routed_to` — in a single atomic update.

**Do not add a routing update after it, and do not fall back to the raw
`gc workflow delete-source ... && gc workflow reopen-source ...` pair.**
Two things go wrong when routing is a separate step you have to remember:

* Pool demand keys on `gc.routed_to`, not on open status and not on
  `gc bd ready` membership. A recovered bead left without it generates zero
  demand — no polecat is spawned for it and none auto-claims it — yet it
  looks entirely healthy in every listing: open, ready, unassigned, branch
  intact on origin. Nothing surfaces it as stuck, so it sits indefinitely
  (gp-982: `ml-b2y.1` reached exactly this state by this path).
* The bead you are salvaging is carrying the dead session's `gc.session_id`,
  `gc.session_name`, `gc.work_dir` and the previous round's `merge_result`.
  Left in the current-round keys those read as this round's answer, and a
  `gc.work_dir` resolved pre-claim points into another agent's worktree —
  possibly the very agent barred from validating the bead (gp-8r1).

If the bead carries `eligible_validators` or `validation_barred_agent`, the
command routes it to `human` rather than the pool and records
`reopen_route_withheld` saying why. That is deliberate: a pool claim never
consults the roster, so pool-routing such a bead would let the barred agent
claim it. Do not override that with a manual route.

**Step 4: Assess and notify.**

Always log each recovery as an event. Mail the mayor only when the
recovery is unexpected or concerning — use your judgment:

| Situation | Action |
|-----------|--------|
| Pool resize / config change removed agent | Log only. Routine. |
| Agent crashed mid-work, branch already on origin | Log + nudge mayor. Low urgency. |
| Work salvaged from worktree (data was at risk) | Log + mail mayor. Work was nearly lost. |
| Same bead recovered before (check `metadata.recovered`) | Log + mail mayor. Possible crash loop. |

When you do mail:
```bash
gc mail send mayor/ -s "ORPHAN_RECOVERED: <bead>" \
  -m "Bead <bead> was assigned to <agent> which is no longer active.
Recovery: <what was done — branch pushed / metadata set / work salvaged / nothing to salvage>
Branch: <branch name or 'none'>
Status: Reset to open pool for re-dispatch.
Concern: <why this one warrants attention>"
```

Mark recovered beads so spawn storm detection can track patterns:
```bash
gc bd update <bead> --set-metadata recovered=true
```

**Exit criteria:** All orphaned beads recovered, worktrees cleaned.
Or no orphans found. Continue to `reap-landed-worktrees` — do NOT exit
the wisp here. The `next-iteration` step is the only place that pours
the next wisp and burns this one."""

[[steps]]
id = "reap-landed-worktrees"
title = "Reap per-bead worktrees whose work already landed"
needs = ["recover-orphaned-beads"]
description = """
Remove per-bead polecat worktrees whose work has reached the target branch.

Runs AFTER `recover-orphaned-beads` on purpose: salvage gets first refusal on
every tree. A tree this step reaps has already been offered to salvage and
declined by it, because salvage only looks at beads with a live-but-unreachable
assignee and these beads are closed and unassigned.

**Why this step exists (gp-a7z).** No component owned a polecat worktree's
death. The refinery deletes the BRANCH after merge, not the tree. Orphan
recovery above removes trees only for orphaned beads. The controller manages
processes, not directories. So the city accumulated one worktree per completed
bead: 5 stale trees when the bug was filed, 8 two days later, roughly 3/day at
that throughput.

**Run the reaper. Do not hand-roll this predicate.**
```bash
gc gastown worktree-reap --reap
```

Reporting is the default and `--reap` is the only mode that deletes; run it
without the flag first if you want to see verdicts before acting.

Exit codes: `0` nothing to report, `1` findings on stdout, `2` the reaper could
not run. A `2` means nothing was measured — that is NOT a clean sweep. Do not
record a reap count for it; treat it as breakage and continue to the next step.

**Read the verdict rows, do not re-derive them.** Column 1 is the verdict:
`reaped` / `reap` / `reap-failed`, the `keep-*` refusals, and `unverifiable`.
Anything `unverifiable` was NOT cleared — it abstained. Never "help" it along by
removing the directory yourself.

**Why the predicate lives in a script and not in this step.** The two checks
that carry the safety argument are `git status --porcelain` empty and
`git cherry <target> HEAD` reporting no `+` commits — together they mean
reaping cannot lose work. Both are exact, and both are easy to approximate
wrongly:

- Ancestry is NOT the landed test. The refinery rebases before merging, so a
  landed branch's commits are on the target under DIFFERENT SHAs. Measured on
  gascity-packs (2026-07-29): of three landed, closed, clean trees, `git
  merge-base --is-ancestor HEAD origin/main` said YES for gp-982 and NO for
  gp-px5 and gp-nrm — all three with zero unmerged patches. An ancestry-keyed
  reaper reaps one and leaks two forever. See gp-zs8 for the same trap in the
  other direction.
- Agent liveness is NOT an input, in either direction. "This agent has no
  session, so its workspace is garbage" would have destroyed gp-px5: a rejected
  bead resumed by a DIFFERENT polecat keeps `metadata.work_dir` pointing into
  the original polecat's workspace, and that agent had no session at all. And
  polecat names are recycled from the namepool — furiosa went from no-session to
  draining in ~15 minutes. The mirror image is just as wrong: `gc session list`
  reports a polecat's `work_dir` as its AGENT WORKSPACE, the parent of every
  per-bead tree, so refusing to reap "under a live agent" would refuse every
  tree of every running polecat and the leak would survive this step entirely.

The reaper keys on the BEAD's terminal state plus a grace window measured from
its `closed_at`, and never touches a parent agent workspace, the rig repo, the
refinery workspace, or a crew checkout.

**Scope discipline.** Do not extend this to agent workspaces. A leaked directory
is a cheap problem; an agent whose workspace vanished mid-session is not.

**Notify only when it is interesting.** Routine reaping is log-only — it is
garbage collection, and mailing about it every cycle is noise. Nudge the mayor
when the pattern is worth a human look:

| Situation | Action |
|-----------|--------|
| Trees reaped, no failures | Log only. Routine. |
| `reap-failed` rows present | Log + nudge mayor. Removal is broken; disk grows. |
| `unverifiable` rows persisting across cycles | Log + nudge mayor. Something is wrong with the bead or the ref, and those trees never get collected. |
| Exit code 2 | Log + mail mayor. The reaper cannot run at all, so the leak is unbounded again. |

```bash
gc session nudge mayor/ "worktree-reap: <n> reaped, <n> failed, <n> unverifiable in $GC_RIG"
```

**Exit criteria:** The reaper ran and its rows were read, or it reported
nothing to do. Continue to `check-unroutable-beads` — do NOT exit the wisp
here. The `next-iteration` step is the only place that pours the next wisp and
burns this one."""

[[steps]]
id = "check-unroutable-beads"
title = "Sweep for open work no pool can claim"
needs = ["reap-landed-worktrees"]
description = """
Assert the routing invariant:

> **open + unassigned + `metadata.branch` present => `gc.routed_to` non-empty**

Pool demand keys on `gc.routed_to` alone — not on open status, not on
`gc bd ready` membership. A bead violating this invariant generates zero
demand: no polecat is spawned for it, none auto-claims it, and it sits
indefinitely. It is invisible to every other check because it looks
healthy from the outside — open, ready, unassigned, branch intact on
origin, often with a good actionable `rejection_reason`. This step exists
because nothing else in the patrol can see it (gp-982).

Rejection and orphan-recovery are the two transitions that predictably
produce this state, so the sweep runs every patrol rather than on demand.

**Step 1: Find violations.**
```bash
gc bd list --status=open --json --limit=0 | jq -r '[ .[] | select((.assignee // "") == "") | select((.metadata["branch"] // "") != "") | select((.metadata["gc.routed_to"] // "") == "") | select((.metadata["halt_reason"] // "") != "auto_push_false") | select(((.metadata["branch_ready"] // "") | tostring) != "true") | select(((.issue_type // .type // "") != "epic")) | {id, title, branch: .metadata["branch"], rejected: (.metadata["rejection_reason"] // ""), recovered: (.metadata["recovered"] // "")} ]'
```

**The two exclusions are load-bearing — do not drop them.**
`halt_reason=auto_push_false` and `branch_ready=true` mark a
*deliberate* halt: `mol-polecat-work` sets `gc.routed_to=""` on purpose
when a bead carries `auto_push=false`, parking the work at branch-ready
for a human. Those beads match the raw shape of the invariant but are
correct. Re-routing them silently resumes work an operator explicitly
opted out of — a worse failure than the one this step fixes, because it
overrides an intent that was stated rather than merely forgotten.

**Step 2: Re-route each violation.**
```bash
gc bd update <bead> --set-metadata gc.routed_to="${GC_RIG:+$GC_RIG/}{{binding_prefix}}polecat"
```
Do not touch the branch, the `rejection_reason`, or anything else. The
branch is intact and the reason is still accurate — routing is the only
thing missing. A new polecat resumes from that branch normally.

**Step 3: Report, because every hit is an upstream bug.**

A violation means some agent's terminal transition dropped the field.
Fixing the bead here clears the symptom; the writer still needs fixing.
Nudge on a single stray, mail on a repeat or a cluster:
```bash
gc session nudge mayor "UNROUTABLE SWEPT: <n> bead(s) open+unassigned with a branch but no gc.routed_to; re-routed to the polecat pool. Suspect a reject/recovery path that does not set routing."
```

If the same bead ID turns up in consecutive patrols, the writer is
actively re-clearing it — that is a code defect, not a stale bead. Mail
the mayor with the bead ID and its `rejection_reason` / `recovered`
fields so the offending path can be traced.

**Exit criteria:** No open+unassigned bead carries a branch without
routing, excluding deliberate `auto_push=false` halts. Continue to
`check-refinery` — do NOT exit the wisp here."""

[[steps]]
id = "check-refinery"
title = "Check refinery queue health"
needs = ["check-unroutable-beads"]
description = """
Ensure the refinery is processing work and the queue is healthy.

**Step 1: Check work beads assigned to refinery:**
```bash
gc bd list --assignee=<rig>/{{binding_prefix}}refinery --status=open --json
```

These are work beads with `metadata.branch` and `metadata.target` that
polecats have submitted for merging. Look for:

- **Stale beads**: Assigned to refinery but old `UpdatedAt`. The refinery
  may be stuck or the bead may have been missed.
- **Queue depth**: Many beads waiting may indicate the refinery is
  overwhelmed or down.
- **Empty queue**: No work assigned — refinery is idle, which is fine.

**Step 2: Assess refinery wisp freshness:**

Check when the refinery last completed a patrol cycle by looking at
recent burned wisps. If the refinery has work assigned but its last
wisp is stale, the refinery may be stuck.

**Step 3: Nudge if needed:**
```bash
gc session nudge <rig>/{{binding_prefix}}refinery "Work beads waiting for merge. Please check queue."
```

**Step 4: Escalate if needed:**
```bash
gc mail send mayor/ -s "QUEUE_HEALTH: <summary>" \
  -m "Work bead IDs: <ids>
Observation: <what you found>
Recommendation: <what should happen>"
```

If the escalation you are about to file claims the merge queue is being
BYPASSED, stop and run Step 5 first. That claim has a specific required
test, and the obvious test is wrong.

**Step 5: Before filing a queue-bypass finding.**

"The merge queue is being bypassed" is a high-cost claim — it names a root
cause a human will act on, against a machine or an operator. Two tests are
required and BOTH must hold. One tempting test is invalid on its own.

**INVALID: commit shape.** Linear single-parent commits on the target, with
no merge commits, are NOT evidence of bypass. The refinery's `direct`
strategy rebases the polecat branch onto the target and fast-forwards
(`git rebase origin/$TARGET`, then `git merge --ff-only`), so a correctly
working queue produces linear single-parent history BY CONSTRUCTION. The
"bypass" shape and the "queue is healthy" shape are the same shape.

Shape does not separate PR merges either: GitHub squash merges are also
linear single-parent, carrying only a `(#NN)` subject suffix. So a `(#NN)`
commit is not proof of a PR merge, and the absence of
`Merge pull request #NN` proves nothing at all.

(gp-zs8: a witness filed a MED escalation off exactly this test and named
"stop bypassing the queue" as the root cause. The two commits it cited as
evidence were its own refinery's merges, made inside its own patrol window.)

**REQUIRED test 1 — the author email is foreign to the town:**
```bash
git log origin/<target> --format='%H %ae %s' -20
# Every user.email configured anywhere in the town:
git worktree list --porcelain | awk '/^worktree /{print $2}' | while read -r wt; do
  git -C "$wt" config user.email
done | sort -u
git config --global user.email
```
An author email appearing in NO town git config is the discriminating
signal. Note that `user.name` is frequently uniform across every agent
worktree, the refinery worktree, and the canonical checkout, because all
of them inherit one global value. Uniform `user.name` means the NAME axis
carries no agency information; it does NOT mean the identity axis is
useless. Always check `user.email` before discarding identity.

**REQUIRED test 2 — no refinery involvement in the reflog:**
```bash
REFINERY_WT=$(git worktree list --porcelain | awk '/^worktree /{print $2}' | grep '/refinery$' | head -1)
git -C "$REFINERY_WT" reflog show HEAD -n 100
git reflog show refs/remotes/origin/<target> -n 50
```
If that grep finds nothing, resolve the refinery worktree from rig config
rather than guessing a path. HEAD is per-worktree, so `git -C <wt> reflog
show HEAD` reads that worktree's own history.

Refinery involvement in its worktree's HEAD reflog looks like
`rebase (pick): <subject>`, `checkout: moving from <sha> to temp`, or
`merge refinery-work: Fast-forward`.

CAVEAT: `rebase (pick)` entries appear ONLY when commits actually needed
replaying. A polecat branch already sitting on top of the target rebases
as a no-op fast-forward and logs no pick entry at all — just the `temp`
checkout pair. Absence of `rebase (pick)` is therefore not evidence of
bypass, and treating it as such is the same mistake in the other
direction.

The `refs/remotes/origin/<target>` reflog is the sharper signal, because
it records HOW the ref moved:
- `update by push` — a process on THIS machine moved it (the refinery did)
- `fetch: fast-forward` — it arrived already-made from somewhere else

A run of `fetch: fast-forward` entries carrying a foreign author email is
the actual shape of a second machine pushing directly. `update by push` is
this town's own refinery doing its job.

Before calling a target commit a lookalike rather than a replay, confirm
patch identity:
```bash
git show <polecat-sha> | git patch-id --stable
git show <target-sha> | git patch-id --stable
```
Equal patch-ids mean the queue replayed that commit. That is the queue
working, not a bypass.

**If both tests hold**, file the finding with the evidence inline: the
foreign author email, the reflog lines, and the patch-id comparison.
**If either test fails, there is no bypass finding to file.**

Even when both hold, prefer the benign reading first: another host in the
same town legitimately doing hardware-bound work it alone can run. That is
a topology fact worth reporting, not a violation to name someone for.

**Exit criteria:** Queue health assessed; nudge sent or escalation
filed if needed. Any queue-bypass claim carries identity + reflog
evidence, or was not filed. Continue to `check-parked-sessions` — do
NOT exit the wisp here. The `next-iteration` step is the only place
that pours the next wisp and burns this one."""

[[steps]]
id = "check-parked-sessions"
title = "Detect sessions parked at a usage-limit prompt"
needs = ["check-refinery"]
description = """
**Config: parked_settle_secs = {{parked_settle_secs}}**

A session parked at a provider usage-limit prompt is a THIRD state, and every
other detector in Gas Town reads it as healthy (gp-px5, measured 2026-07-29):

- `gc session list` reports it `active`, so the controller will not restart it.
- Liveness resolves its assignee to `active`, so `recover-orphaned-beads`
  correctly refuses it. It is NOT an orphan.
- It is not stuck in the `check-polecat-health` sense: no infinite loop, no
  long tool call. It is not executing at all.
- `gc runtime drain` is COOPERATIVE — it sets GC_DRAIN and waits for the agent
  to poll `gc runtime drain-check`. A parked session never polls, so drain is a
  no-op against exactly the case you most want to drain.

Measured cost: a polecat held a P1 bead for 17h and did nothing, its pane
showing a weekly-limit banner and no "esc to interrupt". It was NOT out of
quota — a sibling session was mid-turn on the same shared provider in the same
minute. The banner was stale session state, and `gc session reset` restored it
in under 60 seconds.

It is also a BEAD SINK. Releasing the bead does not help while the session
lives: a bead set to open and unassigned at 19:26:41Z was back in_progress
under the same parked session by 19:29:30Z, with no operator action in
between. Nor can you route around it — a polecat instance is not addressable
(`gc sling <rig>/{{binding_prefix}}<suffix>` fails; the pool target is the
only address), so one parked member can swallow a whole rig's dispatch. Reset
the SESSION. Do not try to move the work.

**Step 1: Measure. Run the detector — do not eyeball panes.**

```bash
gc gastown parked-check
echo "parked check exit: $?"
```

Route it through `gc`, never as a path under `$GC_PACK_DIR`: gc sets that
variable for pack COMMANDS and it is unset in your shell, so a direct path
resolves to `/assets/scripts/...` and measures nothing.

The command scans the active sessions in your own rig (GC_RIG, inherited from
your shell). It peeks each pane, then re-peeks only the sessions showing a
limit banner after one shared {{parked_settle_secs}}s settle gap — the gap is
paid once for the rig, and only when something is actually showing a banner.
Budget roughly 17s for the peek pass on a 5-session rig (measured), plus the
gap when there is a candidate.

Exit 0 means nothing parked and nothing unmeasured. Exit 1 means findings, one
TSV row per session — verdict, session, session_name, alias, rig, detail:

| verdict | meaning | action |
|---------|---------|--------|
| `parked` | banner in the pane tail, no mid-turn marker, pane unchanged across the gap | Step 2 — reset it |
| `settling` | banner present but the pane is still advancing | nothing. It may be auto-retrying; a reset would interrupt a recovery already under way |
| `busy` | a mid-turn marker is present | nothing. It is executing |
| `clear` | no limit banner in the pane tail | nothing |
| `unpeekable` | the peek failed | this session's state was NOT measured. Note it; if the same session is unpeekable across cycles, escalate |

Exit 2 means the check could not run. That is "no session was measured", NOT
health. Escalate the breakage and take no reset action on parked grounds this
cycle:

```bash
gc mail send mayor/ -s "WITNESS: parked-session check cannot run" -m "<the check's stderr>"
```

**Step 2: Reset every `parked` session. This is the documented remedy.**

`gc session reset` is the correct, cheap, bead-preserving fix: the controller
stops the runtime and starts the same session again with fresh provider
conversation state, while session identity, alias, mail, and queued work stay
attached to the existing session bead. The work bead is NOT released and does
NOT need to be — the restarted session resumes it from formula context.

Two things that are NOT the remedy:

- **A warrant.** That sends the dog to kill an agent over a provider budget
  banner, and the shutdown dance's proof-of-life questions can never be
  answered by a session parked at a prompt. Wrong tool, destructive outcome.
- **Releasing the bead.** Measured above: the parked session re-claims it
  within minutes. Reset first; the bead follows the session.

Never reset your own session — you are running this patrol, and a reset
mid-cycle discards your context and the wisp bookkeeping. Skip any row whose
session matches `$GC_SESSION_ID` or `$GC_SESSION_NAME`.

**Precondition — a clean worktree.** Reset restarts the runtime, not the
worktree: files on disk survive. What does not survive is the session's
in-flight reasoning about uncommitted work. Verify the three signals that were
verified in the incident before resetting:

**Check BOTH directory scopes.** A bead records two working directories under
two different keys, and reading only one is how a parked session with a dirty
workspace gets reported as "nothing at risk" (gp-6k8):

| key | scope | written by |
|---|---|---|
| `metadata.work_dir` | the per-bead worktree | polecat, at branch-setup |
| `metadata.gc.work_dir` | the agent's persistent home workspace | gc core, at session stamp |

The second is the PARENT of the first, not another spelling of it. A session
parked BEFORE branch-setup has no `metadata.work_dir` at all — its edits are in
the agent home. So an absent per-bead worktree means "check the home", never
"nothing is at risk".

```bash
BEAD=$(gc bd list --status=in_progress --assignee="$SESSION_NAME" --json --limit=1 | jq -r '.[0].id // empty')
DIRS=''
if [ -n "$BEAD" ]; then
  # Both scopes, in preference order. Deduped so a bead carrying only one, or
  # the same path twice, still probes each real directory exactly once.
  DIRS=$(gc bd show "$BEAD" --json | jq -r '
    .[0].metadata
    | [ (.work_dir // empty), (."gc.work_dir" // empty) ]
    | map(select(. != "")) | unique | .[]')
fi
PROBED=0
for D in $DIRS; do
  [ -d "$D" ] || continue
  PROBED=$((PROBED + 1))
  echo "--- $D (on branch $(git -C "$D" branch --show-current 2>/dev/null || echo '?')) ---"
  git -C "$D" status --porcelain
  git -C "$D" log --oneline origin/main..HEAD
  git -C "$D" stash list
done
echo "probed $PROBED director(ies)"
```

Read the branch from git, as above — never from `metadata.gc.work_branch`. That
field records the base branch (`main`), not the branch checked out in the
directory, so trusting it as the latter misreports every agent home.

`PROBED=0` means no directory existed to measure, which is NOT the same as
verified-clean. Treat it as unverified: if the bead names a path that is gone,
the standing authorization does not apply — mail the mayor as below rather than
resetting on an unmeasured workspace.

All three signals empty across every probed directory (with `PROBED` at least 1)
is a clean workspace: nothing is at risk on disk, and the standing authorization
to reset applies. Reset it:

```bash
gc session reset "$ALIAS"
```

If ANY of the three is non-empty, do NOT reset. There is unpublished work whose
context would be lost. Mail the mayor with the evidence and let this cycle pass:

```bash
gc mail send mayor/ -s "WITNESS: parked session with unpublished work" -m "Session: <alias> (<session_name>)
Bead: <bead>
Banner: <detail column from the check>
Dirty paths / unpushed commits / stashes: <what you found>
Standing authorization covers clean worktrees only — not resetting. Requesting a decision."
```

**Step 3: Record the reset, and never reset-loop.**

Stamp the bead so a repeat is visible to the next cycle and the next witness
session. Metadata is the durable record; your context is not:

```bash
COUNT=$(gc bd show "$BEAD" --json | jq -r '.[0].metadata.parked_reset_count // 0')
gc bd update "$BEAD" --set-metadata parked_reset_count=$((COUNT + 1)) --set-metadata parked_reset_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
```

A session that comes back `parked` after a reset — `parked_reset_count` of 2 or
more — is NOT a stale-banner case. That is a real wall, or something the reset
cannot fix. Do not reset again; escalate:

```bash
gc mail send mayor/ -s "WITNESS: parked session survived reset [HIGH]" -m "Session: <alias>
Bead: <bead>
Resets so far: <count>
Banner: <detail column>
A reset cleared the banner once and it returned. This may be a genuine provider
limit rather than stale session state. Not resetting again."
```

**Step 4: Confirm the recovery.**

Re-measure the session you reset rather than assuming it took. Naming it
explicitly re-checks just that one and bypasses the rig filter:

```bash
gc gastown parked-check "$SESSION"
```

`clear` or `busy` means it came back. Still `parked` on the same cycle usually
means the restart has not finished — leave it for the next cycle, where Step 3's
counter makes a genuine failure visible.

**Exit criteria:** Every active session in the rig measured; every `parked`
verdict either reset under a clean worktree, escalated (dirty worktree), or
escalated as a repeat offender; nothing killed and no bead released on parked
grounds. Or nothing parked. Continue to `check-polecat-health` — do NOT exit
the wisp here. The `next-iteration` step is the only place that pours the next
wisp and burns this one."""

[[steps]]
id = "check-polecat-health"
title = "Check polecat work progress"
needs = ["check-parked-sessions"]
description = """
Detect polecats that are alive but not making progress on their work.

The controller detects dead agents and restarts them. But a polecat can
be alive and stuck — infinite loop, blocked on something, or just not
progressing. The controller can't detect this; it's a work-layer judgment.

**Config: polecat_stale_min = {{polecat_stale_min}}**

**Step 1: Deterministic first pass — measure freshness, never eyeball it.**

Run this before forming any judgment, and do not compute staleness any other
way. Improvised timestamp arithmetic is how this step filed a false warrant
against a healthy polecat (gp-9ly): a `stat` returned a LOCAL-time mtime, the
improvisation formatted it with a `Z` suffix, and compared it against a UTC
`now`. On a PDT host that is a 7-hour error — a file touched 4 minutes earlier
read as ~7h stale, and a polecat running an ordinary 4-minute build/vet/test
cycle was written up as looping.

```bash
GASTOWN_POLECAT_STALE_MIN={{polecat_stale_min}} gc gastown progress-check
echo "progress check exit: $?"
```

Route it through `gc`, never as a path under `$GC_PACK_DIR` — the same rule the
`check-parked-sessions` step states above, for the same reason: gc sets that
variable for pack COMMANDS and it is unset in your shell, so a direct path
resolves to `/assets/scripts/...` and measures nothing. This step shipped that
way (gp-3qb), and the `[ -x ]` guard it sat behind turned the unset variable into
a skip, so the step fell back to the very eyeballing gp-9ly exists to prevent.

The check selects the beads for you: `in_progress`, assignee matching the
polecat role as a delimited token, from your own rig's ledger — so it never
reaches into another rig's polecats. For each, the heartbeat is the NEWER of
the bead's `updated_at` and the newest file mtime under `metadata.work_dir`.
Either signal alone misleads: a polecat editing files for an hour without
touching bd looks stale by bead time, and a polecat mid `go test` touches no
files at all.

It reads those mtimes as raw epoch integers and compares epoch to epoch; the
only formatting happens under `date -u`. **Never format a local mtime with a
`Z` suffix** — a `Z` is a claim the value is UTC. If you ever need a timestamp
by hand, take the bare epoch (`stat -f %m FILE` on macOS, `stat -c %Y FILE` on
Linux) or render UTC explicitly with `date -u -r <epoch> +%Y-%m-%dT%H:%M:%SZ`.
Never pass a file path to `date -r`: it means epoch seconds on BSD and a
filename on GNU, so it is unportable in opposite directions.

Exit 0 means every in-flight polecat is fresh. Exit 1 means findings, one TSV
row per bead — verdict, bead, assignee, age_seconds, heartbeat, source,
commits, dirty:

| verdict | meaning | action |
|---------|---------|--------|
| `fresh` | file or bead activity inside the window | nothing |
| `stale` | no activity in the window — a candidate, NOT a verdict | must be confirmed by Step 2 before anything else |
| `no-heartbeat` | no usable timestamp at all | do NOT warrant on this alone. A freshly-spawned polecat has none yet; only a no-heartbeat that persists across cycles is a signal |

Exit 2 means the check could not run — bad config, an unreadable bead list, or
no usable `stat`. That is "progress was NOT measured", not health. File no
warrants on timestamp grounds this cycle, and escalate the breakage:

```bash
gc mail send mayor/ -s "WITNESS: polecat progress check cannot run" -m "<the check's stderr>"
```

The same rule applies if `gc gastown progress-check` is unavailable at all — an
older pack version that predates the command, say. Either way you have no
staleness measurement this cycle. Proof of life (Step 2) is still available and
is then the only basis on which you may act.

**Step 2: Confirm every `stale` verdict with a live proof-of-life peek.**

A `stale` row means only "no file wrote and no bead updated inside the window".
A polecat blocked in a single long tool call — a big test suite, a cold module
download, a slow rebase — produces exactly that while working perfectly. The
timestamp is a candidate signal; the live session is the evidence.

Peek the target twice, with a gap between:

```bash
gc session peek "$TARGET_SESSION" --lines 40
sleep 30
gc session peek "$TARGET_SESSION" --lines 40
```

A working session's footer carries an elapsed timer and a token counter, e.g.
`✻ Swirling… (5m 49s · ↓ 12.8k tokens)`.

**A rising token counter, or an advancing narrative — new tool calls, new
output — between the two peeks is decisive proof of life and OVERRIDES the
`stale` verdict.** It is a direct observation of the agent working; a timestamp
is only an inference about it. When the two disagree, the observation wins: do
not file a warrant, and do not split the difference by filing one anyway with a
hedged reason.

Only a target that is unchanged across both peeks — same counter, same last
line, no new output — has failed confirmation and may proceed to Step 3.

**Step 3: Rule out normal mid-flight state before calling it stuck.**

None of the following is evidence of a stall:

- **A large uncommitted diff with zero commits.** That is the ordinary shape of
  a long-lived polecat that has not reached its commit step yet. The `dirty`
  and `commits` columns are reported so you can recognize this state, not so
  you can judge it. Many dirty paths with `commits` at 0 is a polecat
  mid-implementation, working exactly as designed.
- **A repeating build/test cadence.** Successive `go build` / `go test` runs a
  few minutes apart are a healthy verification loop. It is only an infinite
  loop if the narrative does not advance — same command, same failure, no new
  reasoning — across peeks taken well apart.
- **Silence during one long tool call.** Step 2 is what settles this.
- **A session `check-parked-sessions` reported as `parked`.** A parked session
  produces the exact evidence this step treats as a confirmed stall — no file
  writes, no bead updates, an unchanged pane across two peeks — because it is
  not executing at all. It is the third state, not a stall, and the previous
  step already reset it or escalated it. Filing a warrant here sends the dog to
  kill an agent over a provider budget banner, and the shutdown dance's
  proof-of-life questions can never be answered by a session parked at a
  prompt. Check the parked verdicts from that step before reading a failed
  proof-of-life peek as a stall.

**Step 4: For confirmed-stuck polecats, file a warrant (deduped).**

Do NOT kill the agent directly. File a warrant bead and let the dog pool
handle the shutdown dance (multi-stage interrogation with due process).

A `stale` row alone never justifies a warrant. Reaching this step requires
both a `stale` verdict from Step 1 and a failed proof-of-life confirmation
from Step 2, and a target that `check-parked-sessions` did not report as
`parked` this cycle. The `reason` must record all three so the dog — and anyone
auditing a pardon later — can see which evidence was actually gathered.

First check for an existing open warrant against the same target — a second
warrant spawns a second shutdown dance racing the first (duplicate kills,
wasted dog cycles). Skip filing if one is already open or in progress:

```bash
TARGET_SESSION="<session-name>"
EXISTING_WARRANT=$(gc bd list --label=warrant --json --limit=0 \
  | jq -r --arg t "$TARGET_SESSION" \
      '[.[] | select((.status == "open" or .status == "in_progress") and (.metadata.target == $t))] | length')
if [ "${EXISTING_WARRANT:-0}" -gt 0 ]; then
  echo "Skipping warrant for $TARGET_SESSION — an open warrant already exists."
else
  gc bd create --type=task \
    --title="Stuck: <session-name>" \
    --metadata '{"target":"<session-name>","reason":"No progress on <bead>: heartbeat <age_seconds>s stale (window {{polecat_stale_min}}m), and token counter unchanged across two peeks 30s apart","requester":"witness","gc.routed_to":"{{binding_prefix}}dog"}' \
    --label=warrant
fi
```

The dog pool picks up the warrant and runs `mol-shutdown-dance`, which
gives the polecat 3 chances to prove it's alive before killing it.

**Step 5: Log assessment:**

Note which polecats were checked and their status for observability.
Don't escalate to mayor for individual stuck polecats — the warrant
system handles it. Only escalate if you see a pattern (many stuck
polecats, systemic issue).

**Exit criteria:** Every in-flight polecat measured by the deterministic
check, every `stale` verdict either overridden by proof of life or escalated
to a warrant, and no warrant filed on a timestamp alone. Or no stale
polecats found. Continue to
`next-iteration` — do NOT exit the wisp here. The `next-iteration` step
is the only place that pours the next wisp and burns this one."""

[[steps]]
id = "next-iteration"
title = "Pour next iteration and loop"
needs = ["check-polecat-health"]
description = """
**Config: event_timeout = {{event_timeout}}**

End of patrol cycle. Pour the next iteration, then wait or exit.

**1. Context check:**

If context feels heavy or RSS is high:
```bash
gc runtime request-restart
```
This blocks forever. The controller restarts you. The next wisp
is already assigned — the new session resumes from context.

**2. Pour next iteration BEFORE burning (reconcile to exactly one):**

Reuse an already-queued wisp instead of pouring a duplicate. A prior cycle
may have poured a next wisp without burning, or a restart may have raced —
keep one open patrol wisp and burn any surplus, so wisps never accumulate.

`gc gastown wisp-reconcile ensure` is the single implementation of that rule; do not
hand-roll the query here. It reconciles the queued wisps to one and pours only
if none is queued.

`--except "$CURRENT"` is load-bearing, not defensive. A wisp is NOT reliably
moved to in_progress while you run it, so the wisp you are running now can
still be `open` — pick it as your own successor, burn it in step 3, and you
are left with ZERO wisps and a patrol loop that dies silently. That is worse
than the duplicate leak: a leak accumulates recoverable surplus, this stops
the engine.
```bash
CURRENT=$(gc gastown wisp-reconcile current) || {
  echo "Could not resolve the current wisp; not pouring and not burning."
  exit 1
}
NEXT=$(gc gastown wisp-reconcile ensure mol-witness-patrol \
         --except "$CURRENT" --var binding_prefix='{{binding_prefix}}') || {
  echo "wisp-reconcile could not guarantee exactly one next wisp — not burning this one."
  exit 1
}
echo "next wisp: $NEXT"
```

A non-zero exit means "do not pour and do not burn", never "nothing found".
Leaving this wisp open is recoverable and visible; burning it without a
successor stops the patrol loop silently.

**3. Burn this wisp:**

Only after `ensure` succeeded above. Burn `$CURRENT` from step 2 — resolving
the id again by hand risks burning something else.
```bash
if [ -n "$CURRENT" ]; then
  gc bd mol burn "$CURRENT" --force
fi
```

**4. Exit this turn:**
Write exactly:
```
IDLE: no work, exiting turn.
```
Then stop immediately. Do NOT sleep, call ScheduleWakeup, or use Monitor.
The session_sleep policy will restart this session after the configured idle interval;
the next wisp is already assigned and the restarted session resumes from it.

**Exit criteria:** Next wisp poured, this wisp burned, turn ended with IDLE signal."""
